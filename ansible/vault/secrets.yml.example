---
# Ansible Vault secrets template
# Copy this file to secrets.yml and encrypt with: ansible-vault encrypt secrets.yml
# Store the vault password in Bitwarden

# Coolify API token (generate from Coolify UI: Keys & Tokens / API tokens)
vault_coolify_api_token: "your-coolify-api-token-here"

# VPS SSH key (optional, if not using default SSH key)
# vault_vps_ssh_key: |
#   -----BEGIN OPENSSH PRIVATE KEY-----
#   ...
#   -----END OPENSSH PRIVATE KEY-----

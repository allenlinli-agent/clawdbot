---
# Example Ansible Vault secrets file
# This file shows the structure of secrets.yml after decryption
# DO NOT commit this file with real secrets!

# Coolify API token (from Coolify UI: Settings → Keys & Tokens → API Tokens)
coolify_api_token: "your_coolify_api_token_here"

# VPS SSH private key (optional - if you need Ansible to store the SSH key)
# Most deployments use ssh-agent or specify the key path in inventory
# Only include this if you need the private key stored in the vault
vps_ssh_private_key: |
  -----BEGIN OPENSSH PRIVATE KEY-----
  your_private_key_content_here
  -----END OPENSSH PRIVATE KEY-----

# To create and encrypt this file:
# 1. Copy this template: cp secrets.yml.example secrets.yml
# 2. Edit with your actual values: vi secrets.yml
# 3. Encrypt the file: ansible-vault encrypt secrets.yml
# 4. Store the vault password in Bitwarden

# To edit later:
# ansible-vault edit secrets.yml

# To decrypt (for viewing only - don't commit decrypted file):
# ansible-vault decrypt secrets.yml
